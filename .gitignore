# Dependencies and build output
node_modules/
/client/dist/
/release*/
/dist/
*.exe
*.blockmap
*.nsis.7z
*.msi
*.zip
*.7z

# Local business data - never commit (the app stores it outside the repo when installed)
/data/
/backups/
/exports/
/logs/
stockpilot.json
!/tools/fixtures/business/data/stockpilot.json
*.jsonl
*.log

# Scratch space used by tests (keep only the placeholder)
/tmp/*
!/tmp/.gitkeep

# Real labels, invoices and scan diagnostics must never be committed
*.pdf
scan-diagnostics*

# Secrets and local configuration
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
*.p12
*.cer
*.crt

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~
